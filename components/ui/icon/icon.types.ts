import type { ComponentType } from "react";
import type { IconProps } from "@solar-icons/react";

/** A Solar icon component (accepts `weight`); the local `Check` glyph is compatible. */
export type Glyph = ComponentType<IconProps>;
